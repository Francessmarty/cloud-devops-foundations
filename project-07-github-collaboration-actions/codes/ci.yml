name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run basic CI checks
        run: |
          echo "CI running..."
          ls -la